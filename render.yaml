services:
  - type: web
    name: banker-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m banker_ai.main server
    envVars:
      - key: PYTHON_VERSION
        value: 3.13
